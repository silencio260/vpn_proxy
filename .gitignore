# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code — launch configs are tracked intentionally
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/
/templates/
/template

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# ─── Environment files (contain secrets — never commit) ──────────────────────
env/
# env.example.json is tracked on purpose — it shows key names but no secrets
!env.example.json

# ─── Firebase (generated by flutterfire configure) ───────────────────────────
lib/firebase_options.dart

# ─── Hive generated type adapters ────────────────────────────────────────────
# (uncomment if you prefer to regenerate rather than commit these)
# **/*.g.dart
