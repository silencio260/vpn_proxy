import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color background = Color(0xff0d0d1a);
  static const Color cardBackground = Color(0xff151422);
  static const Color surfaceBackground = Color(0xff1a1a2e);
  static const Color bottomSheetBackground = Color(0xff20202d);

  // Primary
  static const Color primary = Color(0xff4a90e2);
  static const Color primaryLight = Color(0xff6baef0);
  static const Color primaryDark = Color(0xff2d6db5);

  // Status colors
  static const Color connected = Color(0xff4caf50);
  static const Color connecting = Color(0xffffa726);
  static const Color disconnected = Color(0xff4a90e2);
  static const Color error = Color(0xffe53935);

  // Text
  static const Color textPrimary = Color(0xffffffff);
  static const Color textSecondary = Color(0xffa2a2ae);
  static const Color textHint = Color(0xff666680);

  // Border / divider
  static const Color border = Color(0xff2a2a3e);
  static const Color divider = Color(0xff1e1e30);

  // White
  static const Color white = Colors.white;
}
